"""TestForge orchestration library.

The C++ engine (libtestforge_core + testforge_worker) executes tests.
This Python package is the orchestration layer on top: discovery, scheduling,
selection, regression analysis, failure clustering, storage, reporting,
and the (optional) LLM-backed failure analyst.
"""

__version__ = "0.4.0"
